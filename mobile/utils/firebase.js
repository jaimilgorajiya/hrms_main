import auth from '@react-native-firebase/auth';

/**
 * Native Firebase Authentication instance.
 * For Android/iOS, this uses the native SDK which reads configuration
 * directly from google-services.json.
 */
export { auth };
